import type { Endpoint } from '../types';
import { itemEnvelope, listEnvelope, j, authHeader, paginationParams, pathParam, authHeader as headers, entityId, blockId, branchId, versionId, snapshotId, changesetId } from '../common';

export const versionEndpoints: Endpoint[] = [
  {
    id: 'versions-changesets-list',
    module: 'Versions',
    method: 'GET',
    path: '/versions/changesets',
    summary: 'List changesets',
    description: 'Returns changesets for a branch. Each changeset represents a group of changes with a message.',
    headers: authHeader,
    parameters: [{ name: 'branch_id', type: 'UUID', required: false, description: 'Filter by branch.' }, ...paginationParams],
    response: listEnvelope([{ id: changesetId, branch_id: branchId, message: 'Initial version', created_by: 'd9b23b3f-1d86-4e5a-a5f1-3cf93f9ef294' }]),
  },
  {
    id: 'versions-changesets-create',
    module: 'Versions',
    method: 'POST',
    path: '/versions/changesets',
    summary: 'Create changeset',
    description: 'Creates a changeset, optionally attached to a snapshot. Changesets track what changed and why.',
    headers: authHeader,
    requestBody: j({ branch_id: branchId, snapshot_id: null, message: 'Capture entity edits' }),
    response: itemEnvelope({ id: changesetId, message: 'Capture entity edits' }),
  },
  {
    id: 'versions-snapshots-list',
    module: 'Versions',
    method: 'GET',
    path: '/versions/snapshots',
    summary: 'List snapshots',
    description: 'Returns named snapshots for a branch. Snapshots are point-in-time markers of workspace state.',
    headers: authHeader,
    parameters: [{ name: 'branch_id', type: 'UUID', required: false, description: 'Filter by branch.' }, ...paginationParams],
    response: listEnvelope([{ id: snapshotId, branch_id: branchId, name: 'Before launch' }]),
  },
  {
    id: 'versions-snapshots-create',
    module: 'Versions',
    method: 'POST',
    path: '/versions/snapshots',
    summary: 'Create snapshot',
    description: 'Creates a named snapshot marker for a branch. Snapshots capture the full workspace state at a point in time.',
    headers: authHeader,
    requestBody: j({ branch_id: branchId, name: 'Before launch', description: 'Stable state before production launch' }),
    response: itemEnvelope({ id: snapshotId, name: 'Before launch' }),
  },
  {
    id: 'versions-entity-snapshot',
    module: 'Versions',
    method: 'POST',
    path: '/versions/entities/<entity_id>/snapshot',
    summary: 'Snapshot entity',
    description: 'Captures the current entity state into an entity version record. Optionally attach to a changeset.',
    headers: authHeader,
    parameters: [pathParam('entity_id', 'Entity ID')],
    requestBody: j({ changeset_id: changesetId }),
    response: itemEnvelope({ id: versionId, entity_id: entityId, snapshot: { title: 'Research Notes' } }),
  },
  {
    id: 'versions-entities-list',
    module: 'Versions',
    method: 'GET',
    path: '/versions/entities/<entity_id>',
    summary: 'List entity versions',
    description: 'Returns version history for an entity. Each version is a point-in-time snapshot that can be compared or restored.',
    headers: authHeader,
    parameters: [pathParam('entity_id', 'Entity ID'), ...paginationParams],
    response: listEnvelope([{ id: versionId, entity_id: entityId, snapshot: { title: 'Research Notes' } }]),
  },
  {
    id: 'versions-blocks-list',
    module: 'Versions',
    method: 'GET',
    path: '/versions/blocks/<block_id>',
    summary: 'List block versions',
    description: 'Returns version history for a specific block within an entity.',
    headers: authHeader,
    parameters: [pathParam('block_id', 'Block ID'), ...paginationParams],
    response: listEnvelope([{ id: '5a3978e0-dfeb-475d-911e-5451dd808dad', block_id: blockId, snapshot: { content: { text: 'Intro' } } }]),
  },
  {
    id: 'versions-compare',
    module: 'Versions',
    method: 'GET',
    path: '/versions/compare',
    summary: 'Compare versions',
    description: 'Returns a field-level diff between two entity versions. Shows what changed between left (older) and right (newer).',
    headers: authHeader,
    parameters: [
      { name: 'left_version_id', type: 'UUID', required: true, description: 'Older entity version ID.' },
      { name: 'right_version_id', type: 'UUID', required: true, description: 'Newer entity version ID.' },
    ],
    response: j({
      data: {
        left_version_id: versionId,
        right_version_id: '36fa4408-9cf7-41a8-920e-adc42c91a1d9',
        diff: { title: { left: 'Research Notes', right: 'Research Notes v2' } },
      },
    }),
  },
  {
    id: 'versions-restore',
    module: 'Versions',
    method: 'POST',
    path: '/versions/restore/<version_id>',
    summary: 'Restore entity version',
    description: 'Applies a saved entity version snapshot back onto the live entity. Creates a new version entry for audit trail.',
    headers: authHeader,
    parameters: [pathParam('version_id', 'Entity version ID')],
    response: itemEnvelope({ id: entityId, title: 'Research Notes', restored_from_version_id: versionId }),
  },
];
