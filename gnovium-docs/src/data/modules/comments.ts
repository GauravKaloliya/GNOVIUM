import type { Endpoint } from '../types';
import { itemEnvelope, listEnvelope, j, authHeader, paginationParams, pathParam, authHeader as headers, entityId, blockId, commentId, workspaceId, userId } from '../common';

export const commentEndpoints: Endpoint[] = [
  {
    id: 'comments-list',
    module: 'Comments',
    method: 'GET',
    path: '/comments/',
    summary: 'List comments',
    description: 'Lists all comments, optionally filtered by entity or parent comment (for threaded replies).',
    headers: authHeader,
    parameters: [
      { name: 'entity_id', type: 'UUID', required: false, description: 'Filter comments by entity.' },
      { name: 'parent_comment_id', type: 'UUID', required: false, description: 'List replies for this parent comment.' },
      ...paginationParams,
    ],
    response: listEnvelope([{ id: commentId, entity_id: entityId, content: 'Looks good.', created_by: userId }]),
  },
  {
    id: 'comments-create',
    module: 'Comments',
    method: 'POST',
    path: '/comments/',
    summary: 'Create comment',
    description: 'Creates a comment on an entity, block, or as a reply to another comment. At least one of entity_id or block_id must be provided.',
    headers: authHeader,
    requestBody: j({
      workspace_id: workspaceId,
      entity_id: entityId,
      block_id: null,
      parent_comment_id: null,
      content: 'Looks good.',
    }),
    response: itemEnvelope({ id: commentId, content: 'Looks good.', created_by: userId }),
  },
  {
    id: 'comments-get',
    module: 'Comments',
    method: 'GET',
    path: '/comments/<id>',
    summary: 'Get comment',
    description: 'Retrieves a single comment by ID.',
    headers: authHeader,
    parameters: [pathParam('comment_id', 'Comment ID')],
    response: itemEnvelope({ id: commentId, content: 'Looks good.' }),
  },
  {
    id: 'comments-update',
    module: 'Comments',
    method: 'PATCH',
    path: '/comments/<id>',
    summary: 'Update comment',
    description: 'Updates comment text. The content field is required in the update body.',
    headers: authHeader,
    parameters: [pathParam('comment_id', 'Comment ID')],
    requestBody: j({ content: 'Looks good after review.' }),
    response: itemEnvelope({ id: commentId, content: 'Looks good after review.' }),
  },
  {
    id: 'comments-delete',
    module: 'Comments',
    method: 'DELETE',
    path: '/comments/<id>',
    summary: 'Delete comment',
    description: 'Soft-deletes a comment. Threaded replies are preserved.',
    headers: authHeader,
    parameters: [pathParam('comment_id', 'Comment ID')],
    response: itemEnvelope({ id: commentId, deleted_at: '2026-06-21T12:30:00Z' }),
  },
];
