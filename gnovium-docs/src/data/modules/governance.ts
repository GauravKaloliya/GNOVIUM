import type { Endpoint } from '../types';
import { itemEnvelope, j, authHeader, workspaceParam, workspaceId, entityId, governanceReportId } from '../common';

export const governanceEndpoints: Endpoint[] = [
  {
    id: 'governance-health',
    module: 'Governance',
    method: 'GET',
    path: '/governance/health',
    summary: 'Get workspace health',
    description: 'Returns the latest workspace health report including health score, duplicate count, orphan count, and stale entity count. Does not recalculate — use POST /governance/health-score for fresh data.',
    headers: authHeader,
    parameters: [workspaceParam],
    response: itemEnvelope({
      id: governanceReportId,
      workspace_id: workspaceId,
      health_score: 82,
      duplicate_count: 2,
      orphan_count: 5,
      stale_count: 3,
      report: {
        duplicates: [{ title: 'Untitled', count: 3 }],
        orphans: [{ id: entityId, title: 'Research Notes' }],
        stale: [{ id: entityId, title: 'Research Notes', updated_at: '2025-06-07T12:00:00Z' }],
        entity_count: 42,
      },
      created_at: '2025-06-15T08:30:00Z',
    }),
  },
  {
    id: 'governance-duplicates',
    module: 'Governance',
    method: 'GET',
    path: '/governance/duplicates',
    summary: 'Find duplicates',
    description: 'Returns entities with identical (case-insensitive) titles within a workspace.',
    headers: authHeader,
    parameters: [workspaceParam],
    response: j({ data: [{ title: 'Untitled', count: 3 }, { title: 'Meeting Notes', count: 2 }] }),
  },
  {
    id: 'governance-orphans',
    module: 'Governance',
    method: 'GET',
    path: '/governance/orphans',
    summary: 'Find orphans',
    description: 'Returns entities with zero relations — no incoming or outgoing connections. These entities are isolated from the knowledge graph.',
    headers: authHeader,
    parameters: [workspaceParam],
    response: j({ data: [{ id: entityId, title: 'Research Notes' }] }),
  },
  {
    id: 'governance-stale',
    module: 'Governance',
    method: 'GET',
    path: '/governance/stale',
    summary: 'Find stale entities',
    description: 'Returns entities not updated in 90+ days. Use this to identify knowledge that needs refreshing.',
    headers: authHeader,
    parameters: [workspaceParam],
    response: j({ data: [{ id: entityId, title: 'Research Notes', updated_at: '2025-06-07T12:00:00Z' }] }),
  },
  {
    id: 'governance-health-score',
    module: 'Governance',
    method: 'POST',
    path: '/governance/health-score',
    summary: 'Calculate health score',
    description: 'Forces a fresh health score calculation. Score ranges from 0–100 based on duplicate (×5), orphan (×2), and stale penalties.',
    headers: authHeader,
    parameters: [workspaceParam],
    response: itemEnvelope({
      id: governanceReportId,
      workspace_id: workspaceId,
      health_score: 92,
      duplicate_count: 1,
      orphan_count: 1,
      stale_count: 1,
      report: { duplicates: [], orphans: [], stale: [], entity_count: 42 },
    }),
  },
];
