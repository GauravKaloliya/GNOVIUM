import type { Endpoint } from '../types';
import { itemEnvelope, listEnvelope, j, authHeader, paginationParams, pathParam, workspaceParam, workspaceId, branchId } from '../common';

export const branchEndpoints: Endpoint[] = [
  {
    id: 'branches-list',
    module: 'Branches',
    method: 'GET',
    path: '/branches/',
    summary: 'List branches',
    description: 'Returns version-control branches for a workspace.',
    headers: authHeader,
    parameters: [{ ...workspaceParam, required: false }, ...paginationParams],
    response: listEnvelope([
      { id: branchId, workspace_id: workspaceId, name: 'main', is_default: true },
    ]),
  },
  {
    id: 'branches-create',
    module: 'Branches',
    method: 'POST',
    path: '/branches/',
    summary: 'Create branch',
    description: 'Creates a branch for workspace experimentation or review workflows. Branches are Git-inspired — safe spaces to make changes without affecting the main knowledge base.',
    headers: authHeader,
    requestBody: j({
      workspace_id: workspaceId,
      parent_branch_id: branchId,
      name: 'experiment',
      description: 'Alternative architecture exploration',
      is_default: false,
    }),
    response: itemEnvelope({ id: 'b5a2bf83-1282-4df3-8cb3-1a22bcf291a1', name: 'experiment', is_default: false }),
  },
  {
    id: 'branches-get',
    module: 'Branches',
    method: 'GET',
    path: '/branches/<id>',
    summary: 'Get branch',
    description: 'Retrieves a single branch by ID.',
    headers: authHeader,
    parameters: [pathParam('branch_id', 'Branch ID')],
    response: itemEnvelope({ id: branchId, workspace_id: workspaceId, name: 'main' }),
  },
  {
    id: 'branches-delete',
    module: 'Branches',
    method: 'DELETE',
    path: '/branches/<id>',
    summary: 'Delete branch',
    description: 'Permanently deletes a branch and its associated changesets and snapshots.',
    headers: authHeader,
    parameters: [pathParam('branch_id', 'Branch ID')],
    response: itemEnvelope({ id: branchId, deleted_at: '2026-06-21T12:30:00Z' }),
  },
  {
    id: 'branches-merge-by-id',
    module: 'Branches',
    method: 'POST',
    path: '/branches/<id>/merge',
    summary: 'Merge branch into target',
    description: 'Merges the specified branch into a target branch. Creates a merge changeset capturing the diff.',
    headers: authHeader,
    parameters: [pathParam('branch_id', 'Source branch ID')],
    requestBody: j({ target_branch_id: branchId }),
    response: itemEnvelope({ id: '0b7c95b2-8eb6-4ca4-aa04-420fd68cbb0c', message: 'Merged experiment into main' }),
  },
  {
    id: 'branches-merge',
    module: 'Branches',
    method: 'POST',
    path: '/branches/merge',
    summary: 'Merge two branches',
    description: 'Merges any two branches by specifying both source and target branch IDs explicitly.',
    headers: authHeader,
    requestBody: j({
      source_branch_id: 'b5a2bf83-1282-4df3-8cb3-1a22bcf291a1',
      target_branch_id: branchId,
    }),
    response: itemEnvelope({ id: '0b7c95b2-8eb6-4ca4-aa04-420fd68cbb0c', message: 'Merged experiment into main' }),
  },
];
