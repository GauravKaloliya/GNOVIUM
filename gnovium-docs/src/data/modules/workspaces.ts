import type { Endpoint } from '../types';
import { itemEnvelope, listEnvelope, j, authHeader, paginationParams, pathParam, workspaceId, userId } from '../common';

export const workspaceEndpoints: Endpoint[] = [
  {
    id: 'workspaces-list',
    module: 'Workspaces',
    method: 'GET',
    path: '/workspaces/',
    summary: 'List workspaces',
    description: 'Returns workspaces owned by or accessible to the current user.',
    headers: authHeader,
    parameters: paginationParams,
    response: listEnvelope([
      { id: workspaceId, name: 'Knowledge OS', description: 'Company workspace', owner_id: userId, settings: { default_view: 'graph' } },
    ]),
  },
  {
    id: 'workspaces-create',
    module: 'Workspaces',
    method: 'POST',
    path: '/workspaces/',
    summary: 'Create workspace',
    description: 'Creates a workspace and associates it with the authenticated user as the owner.',
    headers: authHeader,
    requestBody: j({ name: 'Knowledge OS', description: 'Company workspace', settings: { default_view: 'graph' } }),
    response: itemEnvelope({ id: workspaceId, name: 'Knowledge OS', owner_id: userId, settings: { default_view: 'graph' } }),
  },
  {
    id: 'workspaces-get',
    module: 'Workspaces',
    method: 'GET',
    path: '/workspaces/<id>',
    summary: 'Get workspace',
    description: 'Retrieves a single workspace by ID.',
    headers: authHeader,
    parameters: [pathParam('workspace_id', 'Workspace ID')],
    response: itemEnvelope({ id: workspaceId, name: 'Knowledge OS', owner_id: userId }),
  },
  {
    id: 'workspaces-update',
    module: 'Workspaces',
    method: 'PATCH',
    path: '/workspaces/<id>',
    summary: 'Update workspace',
    description: 'Partially updates workspace name, description, or settings. Only send changed fields.',
    headers: authHeader,
    parameters: [pathParam('workspace_id', 'Workspace ID')],
    requestBody: j({ name: 'Knowledge OS Pro', description: 'Production knowledge graph', settings: { default_view: 'table' } }),
    response: itemEnvelope({ id: workspaceId, name: 'Knowledge OS Pro' }),
  },
  {
    id: 'workspaces-delete',
    module: 'Workspaces',
    method: 'DELETE',
    path: '/workspaces/<id>',
    summary: 'Delete workspace',
    description: 'Soft-deletes a workspace. All entities, blocks, and relations are preserved but hidden.',
    headers: authHeader,
    parameters: [pathParam('workspace_id', 'Workspace ID')],
    response: itemEnvelope({ id: workspaceId, deleted_at: '2026-06-21T12:30:00Z' }),
  },
  {
    id: 'workspaces-stats',
    module: 'Workspaces',
    method: 'GET',
    path: '/workspaces/<id>/stats',
    summary: 'Workspace statistics',
    description: 'Returns a curated snapshot of workspace health and activity — entity count, block count, relation count, recent changes, and member stats.',
    headers: authHeader,
    parameters: [pathParam('workspace_id', 'Workspace ID')],
    response: j({
      data: {
        workspace_id: workspaceId,
        entity_count: 42,
        block_count: 215,
        relation_count: 18,
        comment_count: 7,
        member_count: 3,
        archived_count: 5,
        recent_entities: [
          { id: 'fa82a0b1-12c8-47fb-ba2e-ff6a39226cb3', title: 'Research Notes', updated_at: '2026-06-21T08:30:00Z' },
        ],
      },
    }),
  },
];
